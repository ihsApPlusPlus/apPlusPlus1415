using UnityEngine;
using System.Collections;

public class EvilWallScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		renderer.material.color = new Color (255, 0, 0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
